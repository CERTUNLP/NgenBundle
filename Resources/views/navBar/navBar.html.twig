{# 
 This file is part of the Ngen - CSIRT Incident Report System.
 
 (c) CERT UNLP <support@cert.unlp.edu.ar>
 
 This source file is subject to the GPL v3.0 license that is bundled
 with this source code in the file LICENSE.
#}

<!-- Brand and toggle get grouped for better mobile display -->

<nav class="navbar navbar-default navbar-static-top" role="navigation" style="margin-bottom: 0">
    <div class="navbar-header">
        <a class="navbar-brand" href="{{path('cert_unlp_ngen_incident_frontend_home')}}">Ngen</a>
    </div>
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
    </button>
    {% include 'CertUnlpNgenBundle:navBar:navRight.html.twig' %}
    {% include 'CertUnlpNgenBundle:navBar:navSideBar.html.twig' %}
    <!-- /.navbar-static-side -->
    {#        </nav>#}
    {#{% if app.user %}
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse navbar-ex1-collapse">
            {{ knp_menu_render('CertUnlpNgenBundle:MenuBuilder:leftNavbarMenu', { 'style': 'navbar' }) }}
            <form method='post' action="{{path('cert_unlp_ngen_incident_search_incident')}}" class="navbar-form navbar-left" role="search">
                <div class="form-group">
                    <input type="text" name="term" class="form-control" value="{{term | default('')}}" placeholder="Search">
                </div>
                <button type="submit" class="btn btn-default">Submit</button>
            </form>
                
            {{ knp_menu_render('CertUnlpNgenBundle:MenuBuilder:rightNavbarMenu', { 'style': 'navbar-right' }) }}
        </div><!-- /.navbar-collapse -->
    {% endif %}#}
</nav>