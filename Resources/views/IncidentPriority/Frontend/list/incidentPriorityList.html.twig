{# 
 This file is part of the Ngen - CSIRT Incident Report System.
 
 (c) CERT UNLP <support@cert.unlp.edu.ar>
 
 This source file is subject to the GPL v3.0 license that is bundled
 with this source code in the file LICENSE.
#}
<div class="panel panel-default">
    <div class="panel-heading">
        <strong>{{ icon('unlock-alt fa-fw') }}{{ 'Incident Prioritys'|trans }}</strong>
        <strong>({{ objects.getPaginationData.lastItemNumber~"/"~objects.getTotalItemCount }})</strong>
        <div class="pull-right">
            <div class="btn-group">
                <a class="btn btn-default btn-xs" href="{{ path('cert_unlp_ngen_incident_priority_new') }}">
                    {{ icon('plus-circle fa-fw') }}{{ 'add incident Priority'| trans }}
                </a>
            </div>
        </div>
    </div>
    <table class="table table-striped table-bordered table-hover dataTable no-footer">
        <tr role="row">
            <th>{{ knp_pagination_sortable(IncidentPrioritys, 'Name'|trans , 'name') }}</th>
            <th>{{ knp_pagination_sortable(IncidentPrioritys, 'Code'|trans, 'code') }}</th>
            <th>{{ knp_pagination_sortable(IncidentPrioritys, 'Impact'|trans, 'impact') }}</th>
            <th>{{ knp_pagination_sortable(IncidentPrioritys, 'Urgency'|trans, 'urgency') }}</th>
            <th>{{ knp_pagination_sortable(IncidentPrioritys, 'Response Time'|trans, 'response_time') }}</th>
            <th>{{ knp_pagination_sortable(IncidentPrioritys, 'Resolution Time'|trans, 'resolution_time') }}</th>
            <th>{{ knp_pagination_sortable(IncidentPrioritys, 'active'|trans, 'isActive') }}</th>
            <th>{{ 'Action'|trans }}</th>
        </tr>
        <tbody>
        {% for IncidentPriority in IncidentPrioritys %}
            {% include 'CertUnlpNgenBundle:IncidentPriority:Frontend/list/incidentPriorityListRow.html.twig' with {'IncidentPriority': IncidentPriority} %}
        {% endfor %}
        </tbody>

    </table>
</div>
<div class="pagination-centered">
    {{ knp_pagination_render(IncidentPrioritys, null, {}, { "prev_label": "Previous", "next_label": "Next", "first_label": "First", "last_label": "Last"}) }}
</div>
