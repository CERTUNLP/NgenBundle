{# 
 This file is part of the Ngen - CSIRT Incident Report System.
 
 (c) CERT UNLP <support@cert.unlp.edu.ar>
 
 This source file is subject to the GPL v3.0 license that is bundled
 with this source code in the file LICENSE.
#}
    <th class="text-center header">{{ 'Actions'|trans }}</th>
    <th class="text-center header">Time</th>
    <th class="text-center header sortable">{{ knp_pagination_sortable(objects, 'Date'| trans, 'date') }}</th>
    <th class="text-center header sortable">{{ knp_pagination_sortable(objects, 'Address'| trans , 'address') }}</th>
    <th class="text-center header sortable">{{ knp_pagination_sortable(objects, 'Type'| trans, 'type') }}</th>
    <th class="text-center header sortable">{{ knp_pagination_sortable(objects, 'State'| trans, 'state') }}</th>
{#    <th class="text-center header">{{ knp_pagination_sortable(objects, 'TLP'| trans, 'tlp') }}</th>#}
    <th class="text-center header sortable">{{ knp_pagination_sortable(objects, 'Priority'| trans, 'priority') }}</th>
    <th class="text-center header sortable">{{ knp_pagination_sortable(objects, 'Feed'| trans, 'feed') }}</th>
    <th class="text-center header sortable">{{ knp_pagination_sortable(objects, 'Updated'| trans, 'updated') }}</th>
{#    <th class="text-center header">{{ knp_pagination_sortable(objects, 'Reports'| trans, 'ltdCount') }}</th>#}
    <th class="text-center header sortable">{{ knp_pagination_sortable(objects, 'Reporter'| trans , 'reporter') }}</th>
