{# 
 This file is part of the Ngen - CSIRT Incident Report System.
 
 (c) CERT UNLP <support@cert.unlp.edu.ar>
 
 This source file is subject to the GPL v3.0 license that is bundled
 with this source code in the file LICENSE.
#}

<th class="header">{{ knp_pagination_sortable(objects, 'State'| trans, 'state.name') }}</th>
<th class="header">{{ knp_pagination_sortable(objects, 'Info'| trans, '') }}</th>
<th class="header">{{ knp_pagination_sortable(objects, 'Address'| trans , 'origin.ip') }}</th>
<th class="header">{{ knp_pagination_sortable(objects, 'Assigned'| trans , 'assigned.username') }}</th>
<th class="header">{{ knp_pagination_sortable(objects, 'Type'| trans, 'type.name') }}</th>
<th class="header">{{ knp_pagination_sortable(objects, 'Feed'| trans, 'feed.name') }}</th>
<th class="header">{{ knp_pagination_sortable(objects, 'Date'| trans, 'date.date') }}</th>
<th class="header">{{ knp_pagination_sortable(objects, 'Priority'| trans, 'priority.name') }}</th>
<th class="header">{{ knp_pagination_sortable(objects, 'Reportes'| trans, 'ltdCount') }}</th>
<th>{{ 'Actions'|trans }}</th>