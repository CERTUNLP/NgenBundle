{# 
 This file is part of the Ngen - CSIRT Incident Report System.
 
 (c) CERT UNLP <support@cert.unlp.edu.ar>
 
 This source file is subject to the GPL v3.0 license that is bundled
 with this source code in the file LICENSE.
#}

{% set atrasado= incident.priority.resolutiontime-incident.resolutionMinutes %}
 {% if not incident.isNew %}
     {% if not incident.isClosed %}
         {% if incident.resolutionDelayedMinutes > 0 %}
             {{ label_success('Waiting for resolution '|trans ~ incident.resolutionDelayedMinutes ~ ' minutes left'|trans) }}
         {% else %}
             {{ label_danger('Resolution delayed for '|trans ~ (incident.resolutionDelayedMinutes * -1) ~ ' minutes '|trans) }}
         {% endif %}
     {% else %}
         {% if incident.resolutionDelayedMinutes > 0 %}
             {{ label_success('Resolved on time '|trans ~ incident.resolutionDelayedMinutes ~ ' minutes left'|trans) }}
         {% else %}
             {{ label_danger('Resolution delayed for '|trans ~ (incident.resolutionDelayedMinutes * -1) ~ ' minutes '|trans) }}
         {% endif %}

     {% endif %}
 {% endif %}